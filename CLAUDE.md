# Rylees

This is a mono-repo with three sub-projects. Work only inside the relevant sub-project directory.

| Sub-project | Path | Purpose |
|-------------|------|---------|
| API | `src/api/` | Laravel 13 backend — see `src/api/CLAUDE.md` |
| Frontend | `src/frontend/` | Developer Console SPA |
| CLI | `src/cli/` | Release-note publisher CLI tool |
