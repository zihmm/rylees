# Same-origin path; proxied to the API by Vite (see vite.config.js server.proxy).
VITE_API_BASE_URL=/v1


# Origin that serves the public Release History; the scheme drives the link
# href, while the console shows only {slug}.{host}/{key}.
VITE_HISTORY_BASE_DOMAIN=http://rylees.test